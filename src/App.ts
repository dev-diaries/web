import * as React from 'react';
import * as ReactDOM from 'react-dom';
import Search from '@root/components/search';

ReactDOM.render(React.createElement(App), document.getElementsByClassName('js-search')[0]);
